<div>
    <h2>Money</h2>
    <table class="table">
        <thead>
        <tr>
            <td>Coffee addict Name</td>
            <td>Balance</td>
        </tr>
        </thead>
        <tbody>
            {% for item in totals %}
                <tr>
                    <td><a href="{{ path('user_info', {'id': item['id']}) }}">{{ item['name'] }}</a></td>
                    <td>{{ item['credit']/100  }} р.</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>