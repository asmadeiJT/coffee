<nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li><a href="{{ path('homepage') }}">Home</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{ path('add_cup') }}">Add cup</a></li>
                    <li class="dropdown user">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">User<span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a  class="add-user" href="{{ path('add_user') }}">Add User</a></li>
                            <li><a href="{{ path('add_user_credit') }}">Add credit</a></li>
                            <li><a href="{{ path('credit_list') }}">Credit list</a></li>
                        </ul>
                    </li>
                {% endif %}
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Ingredients<span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('add_ingredient') }}">Add Ingredient</a></li>
                            <li><a href="{{ path('add_ingredient_type') }}">Add Type</a></li>
                            <li><a href="{{ path('ingredient_type_list') }}">Types List</a></li>
                        {% endif %}
                        <li><a href="{{ path('ingredient_list') }}">Ingredient List</a></li>
                    </ul>
                </li>
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Settings <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li><a href="{{ path('add_setting') }}">Add Settings</a></li>
                            <li><a href="{{ path('setting_list') }}">Settings List</a></li>
                        </ul>
                    </li>
                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if app.user %}
                    <li><p class="navbar-text">Hello, {{ app.user.username }}</p></li>
                    <li>
                        <a href="{{ path('logout') }}">Logout</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('login') }}">Login</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>