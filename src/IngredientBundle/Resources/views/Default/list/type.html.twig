{% extends 'base.html.twig' %}

{% block title %}Ingredient Types List{% endblock %}

{% block body %}
    <div class="container popover-content">
        <div class="row">
            <div class="col-6 col-md-6">
                <div>
                    <h2>Ingredient Types List</h2>
                    <table class="table">
                        <thead>
                        <tr>
                            <td>Name</td>
                            {% if is_granted('ROLE_ADMIN') %}
                                <td>Action</td>
                            {% endif %}
                        </tr>
                        </thead>
                        <tbody>
                        {% for item in results %}
                            <tr>
                                <td>{{ item.name }}</td>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <td><a href="{{ path('edit_ingredient_type', {'id': item.id}) }}" onclick="return confirm('Are u sure?')">Edit</a></td>
                                {% endif %}
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>


{% endblock %}